{% extends 'TOEICTrainerBundle::base.html.twig' %}

{% block body -%}
    <h1>Change a Picture</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('picturefile') }}">
            Back to the list of Pictures
        </a>
    </li>
    {# <li>{{ form(delete_form) }}</li> #}
</ul>
{% endblock %}
